---
name: abmb-roadside-cicd
description: "ABMB (apac-booking-modernization-backend) CI/CD pipeline layout — cross-region Windows CodeBuild, CodeDeploy to AspireAPAC_Staging, UAT site paths"
metadata: 
  node_type: memory
  type: project
  originSessionId: cd21b4e4-14a3-4c40-aa7d-f65be12cfb5a
---

- ABMB = Bitbucket `internationalsos/apac-booking-modernization-backend` = local `OmnicasaAS/ABMB`. Source lives in `RoadSide/bkkrsa2020-master/` (`BkkIsos47.sln`), .NET Framework 4.8 ASP.NET on IIS. See [[abe-repo-aliases-and-cms-modules]] for sibling repos.
- Pipelines created 2026-07-03 (SIT + UAT, same shape): CodePipeline `roadside-backend-sit` on branch `roadside_release_sit` (underscores — the hyphenated `roadside-release-sit` also exists on origin, user chose underscore one) and `roadside-backend-uat` on `roadside-release-uat` → CodeBuild same-name projects in **us-east-1** (Windows CodeBuild is NOT available in ap-southeast-1 — any Windows build must go cross-region; extra artifact bucket `codepipeline-us-east-1-739075353953-roadside`) → CodeDeploy app `roadside-backend`, groups `roadside-backend-sit` (EC2 tag `DeploySit=roadside-sit`) / `roadside-backend-uat` (tag `Deploy=roadside-uat`). deploy-sites.ps1 switches site maps on DEPLOYMENT_GROUP_NAME. SIT IIS paths: C:\roadside-sit.aspireasia.net, C:\partner.roadside-sit-aspireasia.net, C:\compass.roadside-sit.aspireasia.net.
- CodeBuild uses dedicated role `roadside-backend-uat-codebuild` (reused roles fail: their logs policy is scoped to their own ap-southeast-1 log groups).
- UAT server = EC2 `i-0c0b0ffcdfec95d6f` "AspireAPAC_Staging" (13.213.51.7), Windows 2019, SSM online (use `aws ssm send-command`, no RDP needed), CodeDeploy agent running, shared with uob/limo/booking-form apps. Only C: drive — the `D:\RoadSidePublish` paths in the repo's pubxml files are dev-machine paths, not server paths. UAT IIS sites: `RoadSiteUat` → `C:\roadside-uat-aspireasia.net`, `PartnerRoadsideUat` → `C:\partner.roadside-uat-aspireasia.net`; no Compass UAT site.
- Pipeline files (buildspec.yml, appspec.yml, aws/codedeploy/*.ps1, aws/pipeline-*.json, docs/CICD_SETUP.md) live on the env branch. User pushes to Bitbucket themselves (local git push hangs even with GIT_TERMINAL_PROMPT=0 — ask them to run `! git push ...`).
- SIT pipeline went fully green 2026-07-03 after ~12 iterations. Hard-won facts about the CodeBuild Windows image (windows-base:2022-1.0, VS2019 BuildTools only): msbuild NOT on PATH (locate via vswhere); no Microsoft.WebApplication.targets (NuGet MSBuild.Microsoft.VisualStudio.Web.targets + /p:VSToolsPath); no net48 targeting pack (NuGet Microsoft.NETFramework.ReferenceAssemblies.net48 + /p:TargetFrameworkRootPath); installed SDK 8 needs MSBuild 17 but image has 16 → install .NET SDK 5.0.408 at build time + build-time global.json + `dotnet restore` for the SDK-style MobileConnect; no Web Deploy → use OutDir+WebProjectOutputDir (_CopyWebApplication), NOT DeployOnBuild; no al.exe → /p:GenerateSatelliteAssembliesForCore=true to skip AL, then csc-compile satellites from obj *.resources into final bin\<culture> via a RESPONSE FILE (direct PS-native args to csc get mangled → CS2007) + msbuild /t:_CopyWebApplication as retry. Root Web.config + Web.Debug/Release.config are GITIGNORED (secrets) → CI stubs them; server keeps real config (robocopy /XF Web.config, seed only if absent).
- PowerShell gotchas that broke runs: scripts must be ASCII-only (em-dash mojibake → smart quote terminates PS strings); never @-splat a scalar string to a native command (expands per-character — broke robocopy /XF); CodeDeploy agent runs hooks in 32-bit PS → deploy-sites.ps1 relaunches itself via sysnative for WebAdministration.
- PREPROD added 2026-07-07: IIS sites RoadSidePreprod (C:\roadside-preprod.aspireasia.net) + PartnerRoadsidePreprod (C:\partner.roadside-preprod-aspireasia.net) on same server, HTTPS via *.aspireasia.net wildcard cert (thumbprint ECBEC38A..., expires 2026-12-15); pipeline/CodeBuild/deploy-group all `roadside-backend-preprod`, branch `roadside-release-preprod`, instance tag DeployPreprod=roadside-preprod. DNS for aspireasia.net is in **Cloudflare** (Route53 zone Z0142577313TXEJISQLHI is a stale copy — do NOT add records there); user must add A records in Cloudflare. Preprod Web.configs seeded as copies of UAT configs — must be repointed before real use. Live Web.config backups in Secrets Manager under `roadside/webconfig/{sit,uat}-{roadside,partner}` (binary, 2026-07-03 snapshots).
- Preprod webhook-sync stack built 2026-07-08: branch `roadside-sync-data-preprod` (based on prod+ABE-4868 fix) → pipeline `apac-benefit-sync-data-roadside-preprod` (shared codebuild apac-benefit-backend-codebuild; SSM params under /abe/codepipeline/apac-benefit-sync-data-roadside-preprod/, APPSETTINGS: BKKRSA→BKKRsaPreprodLite, Aspire→preprod-20270707 cluster) → Lambda `apac-benefit-sync-data-roadside-preprod` fronted by HTTP API `nm46bu3aq6` (payload v1, no custom domain) = preprod BenefitWebHookEndPoint. Benefit preprod cluster `preprod-20270707` is usually STOPPED — start it before testing benefit flows. UAT queue apac-benefit-rsa-sync-job-uat.fifo is consumed+deleted by abandoned test lambda `test-dbs-connection` (broken code, hardcoded creds) — reported to user; no prod queue exists, SQS sync path is a dead experiment.
- Preprod separation audit 2026-07-07: OktaRedirectUrl/BaseAppUrl/S3_FolderName+BaseUrlS3Display (roadside-preprod)/SQS (apac-benefit-rsa-sync-job-preprod.fifo, 1-day retention, NO consumer)/partner BucketFolderS3 all preprod-specific now; BenefitWebHookEndPoint stays api-benefit-uat (no benefit-preprod gateway exists). Okta app 0oa1qosl2q2BHUP311d8 needs preprod redirect URI added by an Okta admin. Partner-UAT's BucketFolderS3 points at roadside-production (pre-existing leak, untouched).
- Preprod sites point at **BKKRsaPreprodLite** since 2026-07-07 (old BKKRsaPreprod UAT-clone DROPPED; config backups on server as Web.config.bak-liteswitch). **BKKRsaPreprodLite** built 2026-07-07 on apac-staging (144 MB): full prod BKKRsa schema (64 tables + views + procs, Thai_CI_AS collation, identity preserved, NO FKs) + full reference data (UserAccount/VehService/Vehicle/PayItem/Provider/Client/lookups) + latest-100-jobs coherent slice (jobId is a STRING like 'JB23-...', quote in IN lists). Prod BKKRsa reality: 236 GB file but only 68.6 GB used (167 GB empty — never shrunk); real data half logs (GoogleService 21.6 GB API logs). Build method: dbatools on staging EC2 (installed), script via S3 hop (bucket roadside-db-transfer-739075353953); gotchas: dbatools 2.8.2 Connect-DbaInstance has no -EnableException, force "tcp:host,1433", create DB with source collation. apac-staging is STOPPED nightly ~20:00 Bangkok by instance-scheduler (SIT/UAT/preprod DBs offline evenings).
- Benefit prod DB reachable from user's Mac via SSH tunnel localhost:3382 (Aurora MySQL, db Aspire, creds in secret codepipeline/apac-benefit-client-backend-prod/APPSETTINGS_JSON; lowercase table names). RSA validation incident 2026-07-07: migration abe-migrate-product-addition-method-taxonomy created 281 taxonomy rules at 08:46 UTC without re-materializing privilegeproducts → webhook (WebHookSyncDataRsaBenefit, manual-join only) failed while client backend (live rules) worked; PERMANENTLY FIXED 2026-07-08 (commit 7ae349047/6cdd48b5d, "ABE-4868 Resolve ByRule privilege products live in redemption webhook"): the webhook (WebHookSyncDataRsaBenefit/Repositories/RedeemptionCaseService ValidateProviderRedeemptionCase) now resolves ByRule privilege products LIVE from privilege_product_attribute_rules (ProductTaxonomy: ServiceId/ServiceChildId/ProductTypeId; ProductName: NameEN; all Equals) via new ResolvePrivilegeProductIds helper + new entities PrivilegeProductAttributeRule(Value) + Privilege.ProductAdditionMethod, instead of reading stale privilegeproducts. Deployed to uat then prod; verified stale privs 1126/1127/1128 pass without any sync. So NO bulk re-sync needed anymore. Stopgap per-privilege sync (if ever): POST api/clients/privileges/v3/{id}/product-attribute-rules/sync (gateway needs auth — invoke Lambda apac-benefit-client-backend-prod directly with APIGW proxy event). Note: BkkRsa calls TWO endpoints — MswsController line ~406 (Honda 380/Benz 338 new-job) → webhook api/webhook-roadside/RedeemptionCase/validate-provider-redeemption-case; line ~1039 (redemption save) → client api/clients/privileges/validate-redeemed-roadside (BenefitCallBackWebhook class, confusingly named).
- Only BkkRsa + BkkRsaPartner are deployed (matches user's manual right-click-publish workflow; Compass dropped). C:\RoadsideWeb-Sit-Backup pre-existed with the team's manual `2026_03_12`-style dirs — backup rotation must only touch pipeline-format `yyyy.MM.dd.HH.mm` dirs (underscores sort after dots, so unscoped keep-10 deleted the fresh snapshot).
