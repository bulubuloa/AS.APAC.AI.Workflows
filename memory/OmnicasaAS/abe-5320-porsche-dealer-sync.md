---
name: abe-5320-porsche-dealer-sync
description: "ABE-5320 Porsche dealer sync — verified client-id map, the two-brand gate, 4 pre-existing gaps, DB tunnels and the Playwright suite"
metadata: 
  node_type: memory
  type: project
  originSessionId: 2d90efad-1de1-4c7c-bbe0-1f29c64ee024
  modified: 2026-08-18T08:13:22.546Z
---

ABE-5320 (Sprint 68, Critical): sync Porsche dealers CMS→RSA and allow them as job Client Target. Analysis + plan live at `OmnicasaAS/issues/ABE-5320.md` (artifact https://claude.ai/code/artifact/751a4bde-1d34-49e4-844a-f6c3cdfbce87). Spans ABVB + ABF + ABMB — see [[abe-repo-aliases-and-cms-modules]], [[abmb-roadside-cicd]].

**Verified 2026-08-18 against live prod/UAT (no assumptions):**
- Dealer sync gate is `ABVB VendorService.cs:3086` (`SyncDataToRSADealerVer2`): only `honda_rsa`→`HOT`, `mercedes_benz_rsa`→`MBZ`; the `else` returns **Success with an empty payload**, so an unmapped brand silently never syncs. Legacy v1 twin at `:3024` keyed on masterdata `HONDA_RSA`/`BENZ_RSA`.
- Client id map (Benefit `clients.Id` → `ClientCode` → `ClientIdExternal` = RSA `clientId`) **differs per env**: PROD Honda **38 → HOT → 380**, Benz **68 → MBZ → 338**; SIT Honda **157**, Benz **146** (same codes/externals). `clients.ClientIdExternal` is the durable Benefit→RSA link.
- **ABF `VendorHelpers.cs:217 ClientCmsCodeMapping` keys 157/146 are the SIT ids** → matches on SIT, silently no-ops in prod where GroupCmsCodename falls through to the CMS taxonomy codename (AddVer2.razor:2211). Needs per-env values or deletion, not another hard-coded row.
- Sub-category "Auto Dealer" = existing `masterdatas` MasterCode **DEALEARSHIP_INFOMATION** (prod Id 50125, SIT Id 50267 — **match on MasterCode, ids differ per env**). No seeding needed; `category_module_mappings` → module_roadside_assistance already present in both.
- SIT quirks: its Honda/Benz vendor groups (50584/50585) have **NULL MasterCode**, so the v1 path `SyncDataToRSADealer` (keys on MasterCode) can never fire there; and SIT never populates `vendors.GroupId` (all 27 dealer vendors no-group, 17 still synced) while prod sets it on 257/262. The two envs exercise different code paths.
- Porsche does not exist anywhere yet: no Benefit `clients` row (prod or SIT), no RSA `cloud.Client` row, no `masterdatas` VENDOR_GROUP row, no Kontent `vendor_group_list` item. Codename convention → `porsche_rsa`.
- **Vendor Group dropdown reads Kontent ONLY, never Benefit masterdatas** (ABF `VendorManager.cs:617` → `vendor_group_list`; `InitPageV2`/`VendorService.cs:1468` feeds the Ver2 list filter from CMS too, while the old `InitPage`/`:1404` uses masterdatas). No sync between the two stores. masterdatas VENDOR_GROUP is only reachable via the v1 list, the Excel import (matches `MasterNameEN`, `VendorService.cs:5843`, and does NOT itself sync to RSA), and the v1 `SyncDataToRSADealer`. So creating a group in /masterdatas alone does nothing for vendor creation.
- **Kontent Management API**: repo `appsettings.json` key EXPIRED 2026-02-27; valid keys live in the SSM APPSETTINGS_JSON per env (exp 2028). `vendor_group_list` type id d3d35f02-d925-4355-b7ac-f000355897c0, one required text element `name`. Delivery API caches — pass header `X-KC-Wait-For-Loading-New-Content: true` to see fresh items; variants must be **published** or Delivery won't return them.
- **DONE 2026-08-18**: created + published `porsche_rsa` "Porsche RSA" (item cddd0586-a114-49d0-9f60-75ea97ac0753) in env 225b0999 → live on SIT **and** UAT. Prod (994226e6) deliberately NOT done, pending BA sign-off on the code names.
- **ABVB/pipeline appsettings live in SSM Parameter Store, NOT Secrets Manager**: `/abe/codepipeline/apac-benefit-vendor-backend-{sit,uat,pre-prod,prod}/APPSETTINGS_JSON` (ap-southeast-1). `aws ssm describe-parameters` defaults to 50 results — use `get-parameters-by-path --path /abe/codepipeline --recursive` or you will miss them.
- Kontent environments: **SIT and UAT SHARE `225b0999-fce9-02b4-c44b-ed6990faeeaa`** (8 vendor groups); **PROD = `994226e6-d1a5-023e-0fc9-21571d884f47`** (5 groups: honda_rsa, mercedes_benz_rsa, ngoc_thinh_phat_group, viet_deli, vin_group); **pre-prod points at the PROD env** — creating content "for preprod" writes into prod. Item counts confirm: prod 2380 template_generic vs 2365 prod vendors; 225b0999 1182 vs SIT 977 + UAT 161.
- **Benefit UAT DB = `AspireProdBackup` on the benefit-sit cluster** — the SIT tunnel (3375) reaches it; must use `--ssl-mode=DISABLED` or the handshake drops with "unexpected eof". UAT clients: Honda Id **38**, Benz Id **208** (a third distinct pairing after prod 38/68 and SIT 157/146). UAT+SIT vendor groups both have NULL MasterCode; UAT has 5 dealer vendors, all no-group.

**v1 sync path is STILL LIVE** (user confirmed 2026-08-18) — endpoints `POST api/vendors` / `PUT api/vendors/{Id}` sit beside `.../v2` (VendorController.cs:275,310,286,322). Consequences: `VendorRequestVer2` has **no GroupId field**, so the CMS flow never writes `vendors.GroupId` — only v1 (`VendorService.cs:3433`) and the Excel import (`:5843/:5870`, matched on MasterNameEN) do. Hence prod 257/262 populated vs UAT+SIT 0/32. **Live hazard**: in `SyncDataToRSADealer:2945-2960`, a CMS-created dealer (GroupId NULL, CodeEN set) falls into the `foundMasterData == null` branch and calls `DeleteDealerFromRSA` — it *deletes* the dealer from RSA. Plus a null deref at `:2950` (`.FirstOrDefault(...).MasterCode`, unguarded). Fix by resolving the group from `GroupCmsCodename` in both paths.

**ClientCmsCodeMapping must NOT be keyed on client Id** — the id differs in all three envs. Key on `clients.ClientCode` (HOT/MBZ/POR, stable everywhere); `VendorClientOption` (`VendorService.cs:1090-1108`, ABF `Shared/Results/VendorClientOption.cs`) exposes only ClientId+ClientName today, so ClientCode must be added. Simplest alternative: delete `UpdateGroupCmsCodename` and keep the CMS taxonomy codename (what prod+UAT already rely on).

**Four pre-existing Honda/Benz bugs Porsche exposes** (fix separately from the enablement — shared blast radius): **G1** `ProviderSyncBenefitController.cs:233` sets `ClientId = foundClientId ?? 0` → silent orphan dealers when clientCode is unknown; **G2** `DealerSyncInfoVer2` update branch never writes `bActive`; **G3** `ABVB DeleteVendorByCmsId:3627` / `DeleteVendor:3552` only delete the RSA *provider*, never the dealer (`DeleteRSADealerByVendorCmsId:2953` exists, unused there); **G4** `MswsController_ClientTarget.cs:87` lookup has no `AND bActive=1` — proven live: inactive dealer `10104` was accepted on job 26JB048514.

**Access that worked:** SSH tunnels `localhost:3382` → benefit-prod MySQL (`Aspire`, user benefitAdmin) and `localhost:3383` → rsa-prod MSSQL; RSA creds for SIT/UAT/PROD in Secrets Manager `roadside-connection-string` (UAT = apac-staging/`BKKRsaStaging`/apacadmin). `mysql` at `/opt/homebrew/opt/mysql-client/bin/mysql`, `sqlcmd` on PATH. GAN UAT host is **roadside-uat.aspireasia.net** (not the `roadside-uat-aspireasia.net` IIS folder name). Job Report CSV = `POST /report/jobinfo` with clientId/dtStart/dtEnd; 55 columns today, reflection over `CompleteReportModel` declaration order.

**Test suite:** `Omnicasa.Mobile.ABMB/tests/abe-5320/` (Playwright + mssql, workers:1, reverts its writes). `RSA_TEST_JOB_ID`/`RSA_TEST_CLIENT_ID`/`RSA_PORSCHE_CLIENT_CODE`/`RSA_DB_CONNECTION`; auth via `RSA_USER`+`RSA_PASSWORD` or `RSA_REUSE_STATE=1` with a hand-captured Okta `auth/state.json`. Failing-by-design cases are the ticket's evidence.
